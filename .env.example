# AI Provider — pick ONE and fill it in
# VibeCheck currently uses OpenAI by default (gpt-4o)
OPENAI_API_KEY=sk-proj-...

# To use Anthropic Claude instead, see "Switching to the Claude API" in README.md
# ANTHROPIC_API_KEY=sk-ant-...

DATABASE_URL=sqlite+aiosqlite:///./vibecheck.db
CORS_ORIGINS=http://localhost:5173
